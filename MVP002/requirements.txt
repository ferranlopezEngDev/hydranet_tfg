# Runtime dependencies for the distributed MVP002 pack.
# Validated with Python 3.13 and intended for Python 3.11 or newer.
# `tkinter` is not listed here because it belongs to the Python standard
# library, but on some Linux distributions it must be installed from the
# system package manager (for example `python3-tk` on Debian/Ubuntu).

# Core numerical stack
numpy==2.4.3
scipy==1.17.1

# GUI plotting and embedded Matplotlib toolbars
matplotlib==3.10.8
